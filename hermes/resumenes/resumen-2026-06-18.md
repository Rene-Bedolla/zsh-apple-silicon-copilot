⚠️ El modelo no devolvió contenido en 'content'. Verifica que el servidor MLX en :8000 esté activo (`curl -s http://127.0.0.1:8000/v1/models`) y que el modelo no sea de razonamiento puro sin salida de texto.